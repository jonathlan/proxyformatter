source 'https://rubygems.org'

ruby '2.7.2', :patchlevel => '137'

# Sinatra is in change of handling the web service core
gem 'sinatra', '~> 2.1.0'
# pandoc for Markdown views
gem 'rdiscount'

group :development, :test do
    # Adds support to run the tests
    gem 'rspec', '~> 3.10'
end